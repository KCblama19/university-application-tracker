/**
 * Application-wide JavaScript entry point.
 *
 * Page-specific behavior should live in its own module/file.
 * The sidebar rail behavior, for example, is intentionally kept
 * separate in sidebar-rail.js.
 */
(function () {
  "use strict";

  // Reserved for application-wide behavior that does not belong
  // to a specific page or component.
})();
